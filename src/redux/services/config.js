import { API_URL, STRIPE_PUBLISHABLE_KEY } from 'react-native-dotenv';

export const BASE_API_AUTH_URL = API_URL;
export const BASE_API_URL = `${API_URL}/v1`;
export const PUBLISHABLE_KEY = STRIPE_PUBLISHABLE_KEY;
